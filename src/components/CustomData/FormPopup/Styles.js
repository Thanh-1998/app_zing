import styled from 'styled-components';

const StylesPopper = styled.div`
  width: 100%;
  min-width: 240px;
  box-shadow: 0 4px 6px 0 rgba(32, 33, 36, 0.28);
  background-color: var(--bg-purple);
`;

export default StylesPopper;
