import HomePage from "~/pages/HomePage";
import ChartPage from "~/pages/ChartPage";

const publicroutes = [
  { id: 1, path: '/', component: HomePage },
  { id: 2, path: '/chart', component: ChartPage }
]

export default publicroutes;